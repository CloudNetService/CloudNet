name: CloudNet-NPCs
version: "{project.build.version}"
api-version: "1.13"
author: CloudNetService
website: https://cloudnetservice.eu
description: CloudNet extension which adds NPCs for server selection

main: eu.cloudnetservice.modules.npc.platform.bukkit.BukkitNPCPlugin

depend: [ CloudNet-Bridge ]
softdepend: [ ProtocolLib ]

commands:
  cn:
    aliases: [ cloudnpc ]
    permission: "cloudnet.command.cloudnpc"
    description: "Root command to manage the CloudNet NPC system"
